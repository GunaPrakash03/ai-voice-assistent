# Pin exact patch versions before deploying. The Agents SDK changed
# breakingly between 0.x and 1.x — mixing import styles will not run.
livekit-agents~=1.0
livekit-plugins-deepgram~=1.0
python-dotenv~=1.0

livekit-plugins-silero~=1.0

# Added as the pipeline grows:
#   livekit-plugins-openai    (task 1.4 — LLM)
#   livekit-plugins-cartesia  (task 1.5 — TTS)

